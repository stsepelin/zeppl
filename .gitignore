**/.git/
**/.gitmodules
**/.gitattributes
**/.gitkeep
**/.vscode
**/.claude
**/.gitindex
**/.gitlog
**/.git-*
**/.idea
**/build
**/build-*
**/managed_components
**/dependencies.lock
**/sdkconfig
**/coverage.info
**/coverage.tested.info
**/coverage_html

# simulator screenshot output (VROD_SHOT)
firmware/simulator/*.png
.venv/

# schematics: commit .py + .svg only; the matplotlib perfboard render also emits a .png
docs/schematics/*.png

# enclosure STLs are generated from enclosure.scad (large binaries) — keep .scad + PNG renders, regenerate STLs on demand
hardware/enclosure/*.stl

# Pulled ride logs (raw capture data, not source)
/rides/
